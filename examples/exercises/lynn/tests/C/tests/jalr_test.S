// AUTO-GENERATED FILE — DO NOT EDIT BY HAND
// Generated by gen_rv32_tests.sh
// Christian LamAlvarez
// clamalvarez@hmc.edu
// 2/11/2026

.extern return_jalr_test
.extern fail

.section .text.init
.globl jalr_test
.type  jalr_test, @function

jalr_test:
    test1:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_1
JALR_GET_TGT_1:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_1

        # --- This label's address is what we want to jump to ---
JALR_TARGET_1:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_1

JALR_DO_JALR_1:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_1
JALR_TGT_LINK_1:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_1

        # (control never reaches here)

JALR_CHECK_1:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test2
        jal  x0, Fail_JALR_1

    test2:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_2
JALR_GET_TGT_2:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_2

        # --- This label's address is what we want to jump to ---
JALR_TARGET_2:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_2

JALR_DO_JALR_2:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_2
JALR_TGT_LINK_2:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_2

        # (control never reaches here)

JALR_CHECK_2:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test3
        jal  x0, Fail_JALR_2

    test3:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_3
JALR_GET_TGT_3:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_3

        # --- This label's address is what we want to jump to ---
JALR_TARGET_3:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_3

JALR_DO_JALR_3:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_3
JALR_TGT_LINK_3:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_3

        # (control never reaches here)

JALR_CHECK_3:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test4
        jal  x0, Fail_JALR_3

    test4:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_4
JALR_GET_TGT_4:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_4

        # --- This label's address is what we want to jump to ---
JALR_TARGET_4:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_4

JALR_DO_JALR_4:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_4
JALR_TGT_LINK_4:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_4

        # (control never reaches here)

JALR_CHECK_4:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test5
        jal  x0, Fail_JALR_4

    test5:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_5
JALR_GET_TGT_5:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_5

        # --- This label's address is what we want to jump to ---
JALR_TARGET_5:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_5

JALR_DO_JALR_5:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_5
JALR_TGT_LINK_5:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_5

        # (control never reaches here)

JALR_CHECK_5:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test6
        jal  x0, Fail_JALR_5

    test6:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_6
JALR_GET_TGT_6:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_6

        # --- This label's address is what we want to jump to ---
JALR_TARGET_6:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_6

JALR_DO_JALR_6:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_6
JALR_TGT_LINK_6:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_6

        # (control never reaches here)

JALR_CHECK_6:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test7
        jal  x0, Fail_JALR_6

    test7:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_7
JALR_GET_TGT_7:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_7

        # --- This label's address is what we want to jump to ---
JALR_TARGET_7:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_7

JALR_DO_JALR_7:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_7
JALR_TGT_LINK_7:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_7

        # (control never reaches here)

JALR_CHECK_7:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test8
        jal  x0, Fail_JALR_7

    test8:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_8
JALR_GET_TGT_8:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_8

        # --- This label's address is what we want to jump to ---
JALR_TARGET_8:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_8

JALR_DO_JALR_8:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_8
JALR_TGT_LINK_8:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_8

        # (control never reaches here)

JALR_CHECK_8:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test9
        jal  x0, Fail_JALR_8

    test9:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_9
JALR_GET_TGT_9:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_9

        # --- This label's address is what we want to jump to ---
JALR_TARGET_9:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_9

JALR_DO_JALR_9:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_9
JALR_TGT_LINK_9:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_9

        # (control never reaches here)

JALR_CHECK_9:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, test10
        jal  x0, Fail_JALR_9

    test10:
        addi t3, x0, 0          # flag = 0

        # --- Get address of TARGET into t0 without AUIPC/LUI ---
        jal  t0, JALR_GET_TGT_10
JALR_GET_TGT_10:
        # t0 = PC+4 of the jal above (i.e., address of the next instruction)
        # Now redirect control flow away so we can place TARGET elsewhere.
        jal  x0, JALR_DO_JALR_10

        # --- This label's address is what we want to jump to ---
JALR_TARGET_10:
        addi t3, x0, 1          # flag = 1 (means we landed)
        jal  x0, JALR_CHECK_10

JALR_DO_JALR_10:
        # At this point, t0 holds address of the instruction right after the jal to GET_TGT.
        # That instruction is the 'jal x0, DO_JALR' itself, NOT TARGET.
        # So instead, do another jal that *lands near TARGET* and captures its link.

        jal  t0, JALR_TGT_LINK_10
JALR_TGT_LINK_10:
        # t0 = PC+4 here, and we are positioned immediately before TARGET via fall-through.
        # Place TARGET immediately next so t0 == address of TARGET.
        jal  x0, JALR_TARGET_10

        # (control never reaches here)

JALR_CHECK_10:
        # flag must be 1
        addi t4, x0, 1
        beq  t3, t4, pass
        jal  x0, Fail_JALR_10

    pass:
        jal  x0, return_jalr_test

Fail_JALR_1:
    addi gp, x0, 2047
    addi gp, gp, 354
    jal  x0, fail

Fail_JALR_2:
    addi gp, x0, 2047
    addi gp, gp, 355
    jal  x0, fail

Fail_JALR_3:
    addi gp, x0, 2047
    addi gp, gp, 356
    jal  x0, fail

Fail_JALR_4:
    addi gp, x0, 2047
    addi gp, gp, 357
    jal  x0, fail

Fail_JALR_5:
    addi gp, x0, 2047
    addi gp, gp, 358
    jal  x0, fail

Fail_JALR_6:
    addi gp, x0, 2047
    addi gp, gp, 359
    jal  x0, fail

Fail_JALR_7:
    addi gp, x0, 2047
    addi gp, gp, 360
    jal  x0, fail

Fail_JALR_8:
    addi gp, x0, 2047
    addi gp, gp, 361
    jal  x0, fail

Fail_JALR_9:
    addi gp, x0, 2047
    addi gp, gp, 362
    jal  x0, fail

Fail_JALR_10:
    addi gp, x0, 2047
    addi gp, gp, 363
    jal  x0, fail
