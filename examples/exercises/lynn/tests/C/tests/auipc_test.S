// AUTO-GENERATED FILE — DO NOT EDIT BY HAND
// Generated by gen_rv32_tests.sh
// Christian LamAlvarez
// clamalvarez@hmc.edu
// 2/11/2026

.extern return_auipc_test
.extern fail

.section .text.init
.globl auipc_test
.type  auipc_test, @function

auipc_test:
    test1:
        auipc t2, 0
        jal   t0, AUIPC_AUPC_1
AUIPC_AUPC_1:
        addi  t0, t0, -8          # t0 = PC(auipc)
        addi t3, x0, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test2
        jal   x0, Fail_AUIPC_1

    test2:
        auipc t2, 1
        jal   t0, AUIPC_AUPC_2
AUIPC_AUPC_2:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 1
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test3
        jal   x0, Fail_AUIPC_2

    test3:
        auipc t2, 2
        jal   t0, AUIPC_AUPC_3
AUIPC_AUPC_3:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 2
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test4
        jal   x0, Fail_AUIPC_3

    test4:
        auipc t2, 7
        jal   t0, AUIPC_AUPC_4
AUIPC_AUPC_4:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 7
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test5
        jal   x0, Fail_AUIPC_4

    test5:
        auipc t2, 0x12345
        jal   t0, AUIPC_AUPC_5
AUIPC_AUPC_5:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 74565
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test6
        jal   x0, Fail_AUIPC_5

    test6:
        auipc t2, 0x7ffff
        jal   t0, AUIPC_AUPC_6
AUIPC_AUPC_6:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 524287
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test7
        jal   x0, Fail_AUIPC_6

    test7:
        auipc t2, 0x80000
        jal   t0, AUIPC_AUPC_7
AUIPC_AUPC_7:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 524288
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test8
        jal   x0, Fail_AUIPC_7

    test8:
        auipc t2, 0xfffff
        jal   t0, AUIPC_AUPC_8
AUIPC_AUPC_8:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 1048575
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test9
        jal   x0, Fail_AUIPC_8

    test9:
        auipc t2, 0xffffe
        jal   t0, AUIPC_AUPC_9
AUIPC_AUPC_9:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 1048574
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, test10
        jal   x0, Fail_AUIPC_9

    test10:
        auipc t2, 0xff000
        jal   t0, AUIPC_AUPC_10
AUIPC_AUPC_10:
        addi  t0, t0, -8          # t0 = PC(auipc)
        lui  t3, 1044480
        addi t3, t3, 0
        add   t3, t3, t0          # t3 = PC(auipc) + (imm<<12)
        beq   t2, t3, pass
        jal   x0, Fail_AUIPC_10

    pass:
        jal  x0, return_auipc_test

Fail_AUIPC_1:
    addi gp, x0, 2047
    addi gp, gp, 554
    jal  x0, fail

Fail_AUIPC_2:
    addi gp, x0, 2047
    addi gp, gp, 555
    jal  x0, fail

Fail_AUIPC_3:
    addi gp, x0, 2047
    addi gp, gp, 556
    jal  x0, fail

Fail_AUIPC_4:
    addi gp, x0, 2047
    addi gp, gp, 557
    jal  x0, fail

Fail_AUIPC_5:
    addi gp, x0, 2047
    addi gp, gp, 558
    jal  x0, fail

Fail_AUIPC_6:
    addi gp, x0, 2047
    addi gp, gp, 559
    jal  x0, fail

Fail_AUIPC_7:
    addi gp, x0, 2047
    addi gp, gp, 560
    jal  x0, fail

Fail_AUIPC_8:
    addi gp, x0, 2047
    addi gp, gp, 561
    jal  x0, fail

Fail_AUIPC_9:
    addi gp, x0, 2047
    addi gp, gp, 562
    jal  x0, fail

Fail_AUIPC_10:
    addi gp, x0, 2047
    addi gp, gp, 563
    jal  x0, fail
