// AUTO-GENERATED FILE — DO NOT EDIT BY HAND
// Generated by gen_rv32_tests.sh
// Christian LamAlvarez
// clamalvarez@hmc.edu
// 2/11/2026

.extern return_sb_test
.extern fail

.section .text.init
.globl sb_test
.type  sb_test, @function

sb_test:
    test1:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0xaa; sb t1, 0(t0)
        addi t1, x0, 0xbb; sb t1, 1(t0)
        addi t1, x0, 0xcc; sb t1, 2(t0)
        addi t1, x0, 0xdd; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0x00; sb t1, 0(t2)
        addi t1, x0, 0xbb; sb t1, 1(t2)
        addi t1, x0, 0xcc; sb t1, 2(t2)
        addi t1, x0, 0xdd; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 0; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 0(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_1
1:
        addi sp, sp, 12
        jal  x0, test2

    test2:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0xaa; sb t1, 0(t0)
        addi t1, x0, 0xbb; sb t1, 1(t0)
        addi t1, x0, 0xcc; sb t1, 2(t0)
        addi t1, x0, 0xdd; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0xaa; sb t1, 0(t2)
        addi t1, x0, 0xff; sb t1, 1(t2)
        addi t1, x0, 0xcc; sb t1, 2(t2)
        addi t1, x0, 0xdd; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 255; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 1(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_2
1:
        addi sp, sp, 12
        jal  x0, test3

    test3:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0x00; sb t1, 0(t0)
        addi t1, x0, 0x00; sb t1, 1(t0)
        addi t1, x0, 0x00; sb t1, 2(t0)
        addi t1, x0, 0x00; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0x00; sb t1, 0(t2)
        addi t1, x0, 0x00; sb t1, 1(t2)
        addi t1, x0, 0x7f; sb t1, 2(t2)
        addi t1, x0, 0x00; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 127; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 2(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_3
1:
        addi sp, sp, 12
        jal  x0, test4

    test4:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0x00; sb t1, 0(t0)
        addi t1, x0, 0x00; sb t1, 1(t0)
        addi t1, x0, 0x00; sb t1, 2(t0)
        addi t1, x0, 0x00; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0x00; sb t1, 0(t2)
        addi t1, x0, 0x00; sb t1, 1(t2)
        addi t1, x0, 0x00; sb t1, 2(t2)
        addi t1, x0, 0x80; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 128; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 3(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_4
1:
        addi sp, sp, 12
        jal  x0, test5

    test5:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0x12; sb t1, 0(t0)
        addi t1, x0, 0x34; sb t1, 1(t0)
        addi t1, x0, 0x56; sb t1, 2(t0)
        addi t1, x0, 0x78; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0xab; sb t1, 0(t2)
        addi t1, x0, 0x34; sb t1, 1(t2)
        addi t1, x0, 0x56; sb t1, 2(t2)
        addi t1, x0, 0x78; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 171; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 0(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_5
1:
        addi sp, sp, 12
        jal  x0, test6

    test6:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0x12; sb t1, 0(t0)
        addi t1, x0, 0x34; sb t1, 1(t0)
        addi t1, x0, 0x56; sb t1, 2(t0)
        addi t1, x0, 0x78; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0x12; sb t1, 0(t2)
        addi t1, x0, 0xcd; sb t1, 1(t2)
        addi t1, x0, 0x56; sb t1, 2(t2)
        addi t1, x0, 0x78; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 205; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 1(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_6
1:
        addi sp, sp, 12
        jal  x0, test7

    test7:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0xff; sb t1, 0(t0)
        addi t1, x0, 0xff; sb t1, 1(t0)
        addi t1, x0, 0xff; sb t1, 2(t0)
        addi t1, x0, 0xff; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0xff; sb t1, 0(t2)
        addi t1, x0, 0xff; sb t1, 1(t2)
        addi t1, x0, 0x00; sb t1, 2(t2)
        addi t1, x0, 0xff; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 0; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 2(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_7
1:
        addi sp, sp, 12
        jal  x0, test8

    test8:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0xff; sb t1, 0(t0)
        addi t1, x0, 0x00; sb t1, 1(t0)
        addi t1, x0, 0xff; sb t1, 2(t0)
        addi t1, x0, 0x00; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0xff; sb t1, 0(t2)
        addi t1, x0, 0x00; sb t1, 1(t2)
        addi t1, x0, 0xff; sb t1, 2(t2)
        addi t1, x0, 0x01; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 1; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 3(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_8
1:
        addi sp, sp, 12
        jal  x0, test9

    test9:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0x01; sb t1, 0(t0)
        addi t1, x0, 0x02; sb t1, 1(t0)
        addi t1, x0, 0x03; sb t1, 2(t0)
        addi t1, x0, 0x04; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0x01; sb t1, 0(t2)
        addi t1, x0, 0x02; sb t1, 1(t2)
        addi t1, x0, 0x10; sb t1, 2(t2)
        addi t1, x0, 0x04; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 16; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 2(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_9
1:
        addi sp, sp, 12
        jal  x0, test10

    test10:
        addi sp, sp, -12
        addi t0, sp, 0      # target word   @ sp+0
        addi t2, sp, 4      # expected word @ sp+4
        addi t3, sp, 8      # scratch       @ sp+8

        # init target bytes
        addi t1, x0, 0x10; sb t1, 0(t0)
        addi t1, x0, 0x20; sb t1, 1(t0)
        addi t1, x0, 0x30; sb t1, 2(t0)
        addi t1, x0, 0x40; sb t1, 3(t0)

        # init expected bytes
        addi t1, x0, 0x10; sb t1, 0(t2)
        addi t1, x0, 0xf0; sb t1, 1(t2)
        addi t1, x0, 0x30; sb t1, 2(t2)
        addi t1, x0, 0x40; sb t1, 3(t2)

        # build store value in t1 via scratch bytes then lw
        addi t1, x0, 240; sb t1, 0(t3)
        addi t1, x0, 0; sb t1, 1(t3)
        addi t1, x0, 0;      sb t1, 2(t3)
        addi t1, x0, 0;      sb t1, 3(t3)
        lw   t1, 0(t3)

        # perform store into target
        sb t1, 1(t0)

        # compare lw(target) vs lw(expected)
        lw   t4, 0(t0)
        lw   t5, 0(t2)
        beq  t4, t5, 1f
        jal  x0, Fail_SB_10
1:
        addi sp, sp, 12
        jal  x0, pass

    pass:
        jal  x0, return_sb_test

Fail_SB_1:
    addi gp, x0, 1701
    jal  x0, fail

Fail_SB_2:
    addi gp, x0, 1702
    jal  x0, fail

Fail_SB_3:
    addi gp, x0, 1703
    jal  x0, fail

Fail_SB_4:
    addi gp, x0, 1704
    jal  x0, fail

Fail_SB_5:
    addi gp, x0, 1705
    jal  x0, fail

Fail_SB_6:
    addi gp, x0, 1706
    jal  x0, fail

Fail_SB_7:
    addi gp, x0, 1707
    jal  x0, fail

Fail_SB_8:
    addi gp, x0, 1708
    jal  x0, fail

Fail_SB_9:
    addi gp, x0, 1709
    jal  x0, fail

Fail_SB_10:
    addi gp, x0, 1710
    jal  x0, fail

.section .data
.org 0x1100
.align 2
store_word_1:
    .byte 0xaa, 0xbb, 0xcc, 0xdd
store_word_2:
    .byte 0xaa, 0xbb, 0xcc, 0xdd
store_word_3:
    .byte 0x00, 0x00, 0x00, 0x00
store_word_4:
    .byte 0x00, 0x00, 0x00, 0x00
store_word_5:
    .byte 0x12, 0x34, 0x56, 0x78
store_word_6:
    .byte 0x12, 0x34, 0x56, 0x78
store_word_7:
    .byte 0xff, 0xff, 0xff, 0xff
store_word_8:
    .byte 0xff, 0x00, 0xff, 0x00
store_word_9:
    .byte 0x01, 0x02, 0x03, 0x04
store_word_10:
    .byte 0x10, 0x20, 0x30, 0x40
