// AUTO-GENERATED FILE — DO NOT EDIT BY HAND
// Generated by gen_pipeline_tests.sh
// Christian LamAlvarez
// clamalvarez@hmc.edu

.extern return_branch_flush_test
.extern fail

.section .text.init
.globl branch_flush_test
.type  branch_flush_test, @function

branch_flush_test:
    test1:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_1  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_1:
        addi t4, x0, 0
        beq  t3, t4, test2          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_1

    test2:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_2  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_2:
        addi t4, x0, 0
        beq  t3, t4, test3          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_2

    test3:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_3  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_3:
        addi t4, x0, 0
        beq  t3, t4, test4          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_3

    test4:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_4  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_4:
        addi t4, x0, 0
        beq  t3, t4, test5          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_4

    test5:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_5  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_5:
        addi t4, x0, 0
        beq  t3, t4, test6          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_5

    test6:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_6  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_6:
        addi t4, x0, 0
        beq  t3, t4, test7          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_6

    test7:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_7  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_7:
        addi t4, x0, 0
        beq  t3, t4, test8          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_7

    test8:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_8  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_8:
        addi t4, x0, 0
        beq  t3, t4, test9          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_8

    test9:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_9  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_9:
        addi t4, x0, 0
        beq  t3, t4, test10          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_9

    test10:
        addi t3, x0, 0                # flag = 0
        addi t0, x0, 5
        addi t1, x0, 5
        beq  t0, t1, BR_FLUSH_TAKEN_10  # always-taken (5==5)
        addi t3, x0, 99               # speculatively fetched — must be flushed
        addi t3, x0, 99               # speculatively fetched — must be flushed
BR_FLUSH_TAKEN_10:
        addi t4, x0, 0
        beq  t3, t4, pass          # t3 must still be 0
        jal  x0, Fail_BR_FLUSH_10

    pass:
        jal  x0, return_branch_flush_test

Fail_BR_FLUSH_1:
        addi gp, x0, 801
        jal  x0, fail

Fail_BR_FLUSH_2:
        addi gp, x0, 802
        jal  x0, fail

Fail_BR_FLUSH_3:
        addi gp, x0, 803
        jal  x0, fail

Fail_BR_FLUSH_4:
        addi gp, x0, 804
        jal  x0, fail

Fail_BR_FLUSH_5:
        addi gp, x0, 805
        jal  x0, fail

Fail_BR_FLUSH_6:
        addi gp, x0, 806
        jal  x0, fail

Fail_BR_FLUSH_7:
        addi gp, x0, 807
        jal  x0, fail

Fail_BR_FLUSH_8:
        addi gp, x0, 808
        jal  x0, fail

Fail_BR_FLUSH_9:
        addi gp, x0, 809
        jal  x0, fail

Fail_BR_FLUSH_10:
        addi gp, x0, 810
        jal  x0, fail
